# Copy this file to .env and fill in your values.
# These must also be set in Netlify → Site Settings → Environment Variables.

# Plain-text password used for local dev only (optional if using hash below)
VITE_SITE_PASSWORD=

# SHA-256 hex hash of the site password
VITE_SITE_PASSWORD_HASH=
